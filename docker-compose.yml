# react 専用スタック（S2 — 他構成と同時起動可能）
# ポート既定: Web 8004 / DB 5436 / Vite 5175（.env の APP_HTTP_PORT 等で上書き可）
name: tech-update-task-app-react

services:
  app:
    # docker/php/Dockerfile を変更したら再ビルド: docker compose build app && docker compose up -d
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: tech-update-task-app-react-php
    restart: unless-stopped
    working_dir: /var/www/html
    environment:
      # .env がホスト用でも、コンテナ内は compose の値が使われる（php-fpm clear_env=no）
      APP_URL: http://localhost:${APP_HTTP_PORT:-8004}
      ASSET_URL: http://localhost:${APP_HTTP_PORT:-8004}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_DATABASE: laravel
      DB_USERNAME: postgres
      DB_PASSWORD: secret
    volumes:
      - .:/var/www/html
    networks:
      - app-network
    depends_on:
      postgres:
        condition: service_healthy

  # フロントビルド用（app には Node なし）。常時起動しない — profile node で run する。
  # フロントはホストで npm しない。例: composer npm:docker-build
  # 開発: docker compose --profile node run --rm --service-ports node npm run dev
  node:
    image: node:22-alpine
    container_name: tech-update-task-app-react-node
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
      - node_modules_react:/var/www/html/node_modules
    networks:
      - app-network
    ports:
      - "${NODE_DEV_PORT:-5175}:5173"
    profiles:
      - node

  nginx:
    image: nginx:1.27-alpine
    container_name: tech-update-task-app-react-nginx
    restart: unless-stopped
    ports:
      - "${APP_HTTP_PORT:-8004}:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - app-network
    depends_on:
      - app

  postgres:
    image: postgres:16-alpine
    container_name: tech-update-task-app-react-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: laravel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    ports:
      - "${DB_HOST_PORT:-5436}:5432"
    volumes:
      - postgres_data_react:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d laravel"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data_react:
  node_modules_react:
